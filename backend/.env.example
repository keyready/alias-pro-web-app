TELEGRAM_BOT_TOKEN=123456:replace_with_bot_token
APP_TOKEN_SECRET=replace_with_random_secret
DEV_AUTH=false
# Optional. Defaults to backend/data locally. On Vercel, defaults to /tmp/alias-web-data
# for smoke-test-only ephemeral JSON storage unless explicitly set.
ALIAS_STORAGE_DIR=
