import { Button, Card } from "@heroui/react";
import { useNavigate } from "react-router-dom";
import { useGameStore } from "../features/game/store";

export function HomePage() { const navigate = useNavigate(); const status = useGameStore((s) => s.status); const rematch = useGameStore((s) => s.rematch); return <section className="flex min-h-[80vh] flex-col justify-center gap-4"><div><p className="text-sm uppercase tracking-[.4em] text-[var(--text-secondary)]">Telegram Mini App</p><h1 className="text-6xl font-black tracking-tight">ALIAS</h1></div><Card><Card.Header><Card.Title>Вечеринка начинается</Card.Title><Card.Description>Локальный игровой цикл, общие словари и быстрые свайпы.</Card.Description></Card.Header><Card.Footer className="flex-col gap-3"><Button fullWidth size="lg" onPress={() => navigate("/mode")}>Новая игра</Button><Button fullWidth variant="secondary" onPress={() => navigate("/dictionaries")}>Словари</Button>{status !== "idle" ? <Button fullWidth variant="outline" onPress={() => navigate("/game")}>Продолжить игру</Button> : null}<Button fullWidth variant="ghost" onPress={() => { rematch(); navigate("/mode"); }}>Сыграть ещё раз</Button></Card.Footer></Card></section>; }
